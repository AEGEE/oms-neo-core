language: shell
dist: trusty
sudo: required

before_install:
    - sudo apt-get update
    - sudo apt-get install apt-transport-https ca-certificates curl software-properties-common
    - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
    - sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
    - sudo apt-get update || true
    - sudo apt-get install docker-ce
    - sudo docker run hello-world


before_script:
    - cd ../../
    - git clone --recursive --branch stabilize-derk https://github.com/AEGEE/oms-docker.git AEGEE/oms-docker
    - rm -Rf AEGEE/oms-docker/oms-core
    - mv AEGEE/oms-neo-core AEGEE/oms-docker/oms-core
    - cd AEGEE/oms-docker/docker-dev
    - docker-compose up -d
    - docker-compose logs


script:
    - sudo docker-compose run -T omscore ./vendor/bin/phpunit
