language: php

php:
  - 7.0

before_script:
    - cp .env.testing .env
    - sudo -u postgres createuser homestead
    - composer self-update
    - composer install --no-interaction
    - php artisan key:generate
    - php artisan migrate
    - php artisan db:seed

script:
    - vendor/bin/phpunit
